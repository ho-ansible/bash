# ~/.bashrc
# User init for bash shell
# Stripped-down from Debian default; most config is in /etc/bash/bashrc

# If not running interactively, don't do anything
case $- in
  *i*) ;;
  *) return;;
esac

